// Placeholder — spec 050 implementation lands in the next commit.
// The package scaffolding exists so the spec-050 test suites can run and fail
// for the right reason (missing exports), per the TDD pipeline (Model A).
export {};